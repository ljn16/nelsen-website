/** @type {import('next').NextConfig} */
const nextConfig = {
    // images: {
    //     // Default loader is 'default', which uses Next.js's built-in image optimization
    //     loader: 'default',
    //     // Base path for images; '/' means the root
    //     path: '/app/assets/logos',
    //     // Define device sizes for responsive images
    //     deviceSizes: [320, 420, 768, 1024, 1200],
    //     // Define image sizes for static images
    //     imageSizes: [16, 32, 48, 64, 96],
    //     // You can specify other configurations as needed
    //     // Example: add additional domains if using external images
    //     // domains: ['example.com'],
    //   },
    //   // Other Next.js configurations can go here


};

export default nextConfig;
